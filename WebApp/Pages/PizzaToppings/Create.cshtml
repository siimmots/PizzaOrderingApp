@page
@model WebApp.Pages.PizzaToppings.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PizzaTopping</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PizzaTopping.PizzaId" class="control-label">Pizza</label>
                <select asp-for="PizzaTopping.PizzaId" class ="form-control" asp-items="Model.PizzaIds"></select>
            </div>
            <div class="form-group">
                <label asp-for="PizzaTopping.ToppingId" class="control-label">Topping</label>
                <select asp-for="PizzaTopping.ToppingId" class ="form-control" asp-items="Model.ToppingIds"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
