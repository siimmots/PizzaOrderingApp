@page
@model WebApp.Pages.OrderExtras.IndexModel

@{
    ViewData["Title"] = "Extra toppings";
}

<h1>Extra toppings</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OrderExtra[0].Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderExtra[0].Topping)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OrderExtra) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Order!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topping!.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
